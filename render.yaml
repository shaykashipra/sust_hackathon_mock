services:
  - type: web
    name: customer-triage-api
    runtime: python
    plan: free
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.3
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
